[tox]
envlist = check

[testenv:format]
deps = ruff
skip_install = true
commands =
    ruff check --fix .
    ruff format .

[testenv:check]
deps = ruff
skip_install = true
commands =
    ruff check .
    ruff format --check .
